# Raw Human-Survey Data

The original human-survey microdata from the two replicated studies are **not included
in this repository** per data-use agreements with the original authors.

See `data/README.md` for contact information and data access instructions.

Placeholder directories `study1_nces/` and `study2_hwa/` are provided so the
directory structure matches the replication scripts in `code/analysis/`.
